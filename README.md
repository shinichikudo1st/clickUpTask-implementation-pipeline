# clickUpTask-implementation-pipeline
a Go service that automatically creates implementation milestone/phase markdown files when a new ClickUp task is assigned. It stores the generated file and metadata in Supabase, and sends the result to the owner by email.
